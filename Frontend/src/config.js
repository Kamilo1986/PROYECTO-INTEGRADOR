const config = {
    apiUrl: process.env.REACT_APP_API_URL || "http://localhost:5000",
    appName: process.env.REACT_APP_NAME || "Tu Hotel",
    environment: process.env.REACT_APP_ENV || "development",
  };
  
  export default config;
  